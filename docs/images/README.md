# Doc images

Drop screenshots and diagrams referenced by `docs/*.md` files here.

Expected files:

- `chrome-flags-webmcp.png` - screenshot of `chrome://flags?search=mcp` with both `WebMCP for testing` and `WebMCP support in DevTools` set to **Enabled**. Referenced by [`docs/browser-support.md`](../browser-support.md).
